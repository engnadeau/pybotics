name: Test

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: "3.x"
      - name: Install system dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry
      - name: Install package dependencies
        run: |
          poetry install
      - name: Run linting checks
        run: |
          make lint

  test-examples:
    runs-on: ubuntu-latest
    strategy:
      matrix:
<<<<<<< HEAD
        python-version: [3.7, 3.8]
=======
        python-version:
          - 3.7
          - 3.8
          - 3.9
          - 3.10
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install system dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry
      - name: Install package dependencies
        run: |
          poetry install --no-dev
      - name: Test example
        run: |
          poetry run python examples/basic_usage.py
>>>>>>> ci: cleaned test workflow

  test-unit:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version:
          - 3.7
          - 3.8
          - 3.9
          - 3.10
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install system dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry
      - name: Install package + dev dependencies
        run: |
          poetry install
      - name: Unit testing
        run: |
          make test

  test-notebooks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v2
        with:
          python-version: "3.x"
      - name: Install system dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry
      - name: Install package + notebook dependencies
        run: |
          poetry install
      - name: Test notebooks
        run: |
          make test-notebooks
